# Команды для работы с инсталлятором TaskMaster

## 📦 Установка

### Обычная установка
```cmd
TaskMaster-Installer-1.0.2.exe
```
Запускает графический интерфейс установки с возможностью выбора пути установки.

### Тихая установка (без диалогов)
```cmd
TaskMaster-Installer-1.0.2.exe /SILENT
```
или короткая форма:
```cmd
TaskMaster-Installer-1.0.2.exe /S
```
Устанавливает программу в стандартную директорию (`C:\Program Files\TaskMaster`) без показа диалогов.

### Тихая установка с указанием пути
```cmd
TaskMaster-Installer-1.0.2.exe /SILENT /DIR="C:\MyPrograms\TaskMaster"
```
Устанавливает в указанную директорию без показа диалогов.

## 🗑️ Удаление

### Обычное удаление
```cmd
"C:\Program Files\TaskMaster\uninstall.exe"
```
Запускает процесс удаления с подтверждением.

### Тихое удаление
```cmd
"C:\Program Files\TaskMaster\uninstall.exe" /SILENT
```
или:
```cmd
"C:\Program Files\TaskMaster\uninstall.exe" /S
```
Удаляет программу без показа диалогов.

## 🔍 Проверка установки

### Проверка через реестр
```cmd
reg query "HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall\TaskMaster"
```

### Проверка наличия файлов
```cmd
dir "C:\Program Files\TaskMaster\TaskMaster.exe"
```

## 📝 Примеры использования

### Автоматическая установка через скрипт
```cmd
@echo off
echo Установка TaskMaster...
TaskMaster-Installer-1.0.2.exe /SILENT
if %ERRORLEVEL% EQU 0 (
    echo Установка завершена успешно!
) else (
    echo Ошибка установки!
)
pause
```

### Установка с последующим запуском
```cmd
TaskMaster-Installer-1.0.2.exe /SILENT && "C:\Program Files\TaskMaster\TaskMaster.exe"
```

### Обновление (удаление старой версии и установка новой)
```cmd
@echo off
if exist "C:\Program Files\TaskMaster\uninstall.exe" (
    "C:\Program Files\TaskMaster\uninstall.exe" /SILENT
    timeout /t 2 /nobreak >nul
)
TaskMaster-Installer-1.0.2.exe /SILENT
```

### Массовая установка на несколько компьютеров
```cmd
@echo off
for /f %%i in (computers.txt) do (
    echo Установка на %%i...
    psexec \\%%i -s TaskMaster-Installer-1.0.2.exe /SILENT
)
```

## ⚙️ Параметры командной строки

| Параметр | Описание |
|----------|----------|
| `/SILENT` или `/S` | Тихая установка/удаление без диалогов |
| `/DIR="путь"` | Указать путь установки (только для установщика) |

## 📌 Примечания

- Для тихой установки требуются права администратора
- При тихой установке программа устанавливается в `C:\Program Files\TaskMaster` по умолчанию
- Ярлык на рабочем столе создается автоматически
- При обнаружении существующей установки она автоматически удаляется перед установкой новой версии

## 🛠️ Устранение проблем

### Инсталлятор не запускается
```cmd
# Проверьте права администратора
whoami /priv

# Запустите от имени администратора
```

### Ошибка доступа к файлам
```cmd
# Закройте все запущенные экземпляры TaskMaster
taskkill /F /IM TaskMaster.exe

# Затем повторите установку/удаление
```

### Очистка остатков после удаления
```cmd
# Удаление вручную
rmdir /s /q "C:\Program Files\TaskMaster"

# Очистка реестра (осторожно!)
reg delete "HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall\TaskMaster" /f
```
