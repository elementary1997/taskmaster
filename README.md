# ✨ Modern Task Manager

Современный легковесный менеджер задач с полупрозрачным интерфейсом и минималистичным дизайном.

## 🎨 Особенности

- **Современный дизайн** - glassmorphism эффект с градиентным фоном
- **Полупрозрачность** - окно с настраиваемой прозрачностью (96%)
- **Масштабируемое окно** - можно изменять размер и размещать в любой части экрана
- **Минималистичный интерфейс** - только необходимые функции, без лишнего
- **Перетаскивание** - легко перемещать окно по экрану
- **Приоритеты задач** - высокий (красный), средний (жёлтый), низкий (зелёный)
- **Быстрое добавление** - добавление задачи по Enter
- **Автосохранение** - все задачи сохраняются в JSON

## 📋 Требования

- Python 3.8 или выше
- PySide6

## 🚀 Установка и запуск

### 1. Установите зависимости

```bash
pip install PySide6
```

### 2. Запустите приложение

```bash
python modern_task_manager.py
```

## 💡 Использование

### Добавление задачи
1. Введите название задачи в поле ввода
2. Выберите приоритет (⚡ Высокий / ⭐ Средний / ✓ Низкий)
3. Нажмите "+ Добавить" или клавишу Enter

### Управление задачами
- **✓ Отметить выполненной** - нажмите зелёную кнопку с галочкой
- **↺ Вернуть в работу** - нажмите кнопку с круговой стрелкой на выполненной задаче
- **× Удалить** - нажмите красную кнопку с крестиком
- **Редактировать** - дважды кликните по задаче (текст появится в поле ввода)

### Настройка окна
- **Перемещение** - зажмите левую кнопку мыши на окне и перетащите
- **Изменение размера** - потяните за правый нижний угол окна
- **Закрытие** - нажмите красную кнопку × в правом верхнем углу

## 🎯 Цветовая индикация

- **Красная полоса** (⚡) - высокий приоритет
- **Жёлтая полоса** (⭐) - средний приоритет  
- **Зелёная полоса** (✓) - низкий приоритет
- **Серый текст** - выполненная задача (зачёркнутая)

## 📁 Хранение данных

Все задачи сохраняются в файле `tasks.json` в формате:

```json
[
  {
    "id": 1,
    "title": "Название задачи",
    "description": "",
    "priority": "high",
    "status": "Не выполнено",
    "due_date": "",
    "created": "29.01.2026 17:30"
  }
]
```

## 🔧 Технические детали

- **Фреймворк**: PySide6 (Qt)
- **Дизайн**: Glassmorphism с градиентным фоном
- **Прозрачность**: 96% (настраивается в коде)
- **Размер по умолчанию**: 380×600 пикселей
- **Минимальный размер**: 320×400 пикселей
- **Режим**: Always on top (поверх других окон)

## 📚 Старые версии

В проекте также доступны предыдущие версии:

- `task_manager.py` - версия на Tkinter/CustomTkinter с расширенным функционалом
- `task_manager_qt.py` - полнофункциональная версия на PySide6

Для запуска старых версий используйте соответствующие команды:
```bash
python task_manager.py
# или
python task_manager_qt.py
```

## 🎨 Дизайн-система

### Цвета
- Фон: градиент от `#1a1a2e` до `#16213e`
- Карточки: полупрозрачный белый `rgba(255, 255, 255, 0.08)`
- Текст: белый с различной прозрачностью
- Акценты: яркие цвета для приоритетов

### Шрифты
- Основной: Segoe UI (системный для Windows)
- Размеры: 18pt (заголовок), 11pt (задачи), 10pt (кнопки)

## 📦 Сборка приложения

### Windows (.exe)

Для создания автономного исполняемого файла `.exe`:

1. Убедитесь, что установлен `pyinstaller`:
   ```bash
   pip install pyinstaller
   ```
2. Запустите скрипт сборки:
   ```bash
   python build_windows.py
   ```
3. Готовый файл будет находиться в папке `dist/`.

### Linux (.deb)

Для создания Debian-пакета (требуется окружение Linux):

1. Убедитесь, что установлены необходимые инструменты:
   ```bash
   sudo apt-get install dpkg-dev
   ```
2. Запустите скрипт сборки:
   ```bash
   chmod +x build_deb.sh
   ./build_deb.sh
   ```
3. Готовый пакет `.deb` будет создан в папке `build_deb/`.

3. Готовый пакет `.deb` будет создан в папке `build_deb/`.

## 💾 Где хранятся данные?

Поскольку приложение является портативным (portable) или устанавливаемым, все задачи сохраняются в специальной папке пользователя, чтобы не потеряться при обновлении программы.

- **Windows**: `C:\Users\ВашеИмя\AppData\Local\ModernTaskManager\tasks.json`
- **Linux**: `~/.local/share/ModernTaskManager/tasks.json`

Если вы хотите перенести свои задачи на другой компьютер, просто скопируйте файл `tasks.json` в соответствующую папку на новом устройстве.

## 🔊 Звуки

### Включение/выключение звуков

На главном экране в нижней панели есть кнопка 🔊/🔇 для включения и выключения звуков. Настройка сохраняется автоматически.

### Пользовательский звук

Вы можете использовать свой собственный WAV-файл для звука выполнения задачи:

1. Создайте папку `audio` в директории приложения (рядом с `modern_task_manager.py`)
2. Поместите ваш WAV-файл в эту папку и назовите его `custom.wav`
3. При выполнении задачи будет проигрываться ваш звук

**Приоритет звуков:**
1. `audio/custom.wav` - ваш пользовательский файл (высший приоритет)
2. `audio/click.wav` - сгенерированный щелчок (если запустить `generate_sound.py`)
3. Программный щелчок - если файлов нет, используется встроенный звук

**Пример:**
```
taskmanager/
├── modern_task_manager.py
├── audio/
│   └── custom.wav  ← Ваш звук здесь
└── ...
```

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

---

**Создано с ❤️ для продуктивной работы**
